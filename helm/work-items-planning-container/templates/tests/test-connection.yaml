apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "work-items-planning-container.fullname" . }}-test-connection"
  labels:
    {{- include "work-items-planning-container.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "work-items-planning-container.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
