apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "work-items-planning.fullname" . }}-test-connection"
  labels:
    {{- include "work-items-planning.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "work-items-planning.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
