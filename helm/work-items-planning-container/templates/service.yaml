apiVersion: v1
kind: Service
metadata:
  name: {{ include "work-items-planning.fullname" . }}
  labels:
    {{- include "work-items-planning.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "work-items-planning.selectorLabels" . | nindent 4 }}
