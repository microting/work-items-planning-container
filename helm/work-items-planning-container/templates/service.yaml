apiVersion: v1
kind: Service
metadata:
  name: {{ include "work-items-planning-container.fullname" . }}
  labels:
    {{- include "work-items-planning-container.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "work-items-planning-container.selectorLabels" . | nindent 4 }}
